<%= form_for(@ride, validate: true) do |f| %>
  <% if @ride.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ride.errors.count, "error") %> prohibited this ride from being saved:</h2>

      <ul>
      <% @ride.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ridename %><br />
    <%= f.text_field :ridename %>
  </div>
  
  <div class="field">
       <%= f.label :picture %><br />
       <%= f.text_field :picture %>
	   
  </div>
  
  <div class="field">
    <%= f.label :video %><br />
    <%= f.text_area :video %>
  </div>
  
  <div class="field">
   <%= f.label :How_many_people_in_the_ride%><br />
    <%= f.select :type_id, ["1","2" , "3" , "4" , "5"] %> 
  </div>
  
    <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field:price %>
  </div>
  
  
      <button type="reset" value="Reset">Clear</button>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
